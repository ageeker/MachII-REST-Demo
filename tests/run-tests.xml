<?xml version="1.0" encoding="ISO-8859-1"?>
<project name="MXUnitTask" basedir="." default="run">

	<property name="mxunit.jar" value="${mxunit.systempath}/ant/lib/mxunit-ant.jar" />
	<taskdef name="mxunittask" classname="org.mxunit.ant.MXUnitAntTask" classpath="${mxunit.jar}" />

	<property name="server.name" value="${edgelearn.server.name}" />
	<property name="server.port" value="${edgelearn.server.port}" />

	<target name="run" depends="runTests" />

	<target name="runTests" description="Make output directories and run the MXUnit task">
		<mxunittask server="${server.name}" port="${server.port}" defaultrunner="/mxunit/runner/HttpAntRunner.cfc" verbose="true" haltonfailure="true" haltonerror="true">
			<directory 
				runner="/mxunit/runner/HttpAntRunner.cfc"
				remoteMethod="run"
				path="/EdgeLearn-Tests"
				packageName="EdgeLearn-Tests"
				componentPath="EdgeLearn-Tests"
				recurse="false"
			/>
		</mxunittask>
	</target>

</project>
